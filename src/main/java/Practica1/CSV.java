package Practica1;

import java.util.Scanner;

public class CSV {
    public Table readTable(String){
        Scanner sc = new Scanner(System.in);

        //Lee la tabla del excel (String nombre tabla)
        //Primera fila
        //Guardar info en List<String> header

        //Por cada fila que quede
        /*Crear un List<Double>
        Pone ese List<Double> en un row
        Ese Row lo añade a un Array de Rows
         */
        //Cuando se acabe creamos table (tenemos lista headers y lista rows)
        //Devolvemos table
    }
}
